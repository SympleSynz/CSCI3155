const r1 = 1.01;
const r2 = 1.01;
const f = function f(x) { jsy.print(r1);
                const temp = r2;
                const r2 = r1 + r2;
                const r1 = temp;
                return x === 0 ? 0 : f(x - 1) };
f(5)
jsy.print(r1);
jsy.print(r2)
